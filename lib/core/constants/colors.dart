import 'package:flutter/material.dart';

class AppColors {
  final Color selectedColor = Colors.orange.withOpacity(0.3);
  final Color unselectedColor = Colors.blueGrey;
  final Color unselectedItemColor = const Color.fromARGB(255, 211, 44, 44);
  final Color? amberColor = Colors.amber[800];
  final Color blackColor = Colors.black;
}
